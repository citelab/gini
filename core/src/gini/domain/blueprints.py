"""DEPRECATED — the requirement chip / auto-build Wizard was replaced by the goal-guided
Wizard (see ``domain/missions.py``). This module is intentionally empty and can be
deleted; it is kept only because the sandbox can't remove files (delete on the host:
``git rm src/gini/domain/blueprints.py``).
"""
