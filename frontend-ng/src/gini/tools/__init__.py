"""Developer tools that ship with gini-toolkit but are never imported by the app.

Each one is runnable with `python -m gini.tools.<name>`. They exist because some bugs can only
be settled with the real bytes from a real session, and a student can produce those where a
maintainer cannot.
"""
