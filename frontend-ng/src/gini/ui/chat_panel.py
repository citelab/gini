"""RETIRED. Messaging is no longer a separate dock — it folded into the Ask GINI panel as one
unified conversation surface (GINI + people, one transcript, a conversation switcher). See
`assistant.py` (`_select_convo`, `_render_convo`, `_send_human`).

This module is intentionally empty; it remains only so any stale import fails loudly rather than
silently resurrecting a second chat panel.
"""
